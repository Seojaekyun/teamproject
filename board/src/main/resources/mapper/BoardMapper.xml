<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.board.mapper.BoardMapper">
	<select id="list" resultType="kr.co.board.dto.BoardDto">
		select * from pageboard order by id desc limit #{index},#{rec}
	</select>
	<select id="getChong" resultType="Integer">
		select ceil( count(*)/#{param1} ) from pageboard
	</select>
	<insert id="writeOk" parameterType="kr.co.board.dto.BoardDto">
		insert into pageboard(title,name,pwd,content,writeday)
		values(#{title},#{name},#{pwd},#{content},now())
	</insert>
	<update id="rnum">
		update pageboard set rnum=rnum+1 where id=#{id}
	</update>
	<select id="content" resultType="kr.co.board.dto.BoardDto">
		select * from pageboard where id=#{id}
	</select>
	<select id="update" resultType="kr.co.board.dto.BoardDto">
		select * from pageboard where id=#{id}
	</select>
	<update id="updateOk" parameterType="kr.co.board.dto.BoardDto">
		update pageboard set
		name=#{name}, title=#{title}, content=#{content} where id=#{id}
	</update>
	<select id="isPwd" resultType="Boolean">
		select count(*) from pageboard where id=#{id} and pwd=#{pwd}
	</select>
	<delete id="delete">
		delete from pageboard where id=#{id}
	</delete>
	<select id="dat" resultType="kr.co.board.dto.DatDto">
		select * from dat where bid=#{id} order by id desc
	</select>
	<insert id="datOk" parameterType="kr.co.board.dto.DatDto">
		insert into dat(bid, nick, pwd, content, writeday) values(#{bid}, #{nick}, #{pwd}, #{content}, now())
	</insert>
	<select id="datUp" resultType="kr.co.board.dto.DatDto">
		select * from dat where id=#{param1}
	</select>
	<select id="isdatPwd" resultType="boolean">
		select count(*) from dat where id=#{param1} and pwd=#{param2}
	</select>
	<update id="datupOk" parameterType="kr.co.board.dto.DatDto">
		update dat set nick=#{nick}, content=#{content} where id=#{id}
	</update>
	<delete id="datDel" parameterType="kr.co.board.dto.DatDto">
		delete from dat where id=#{id}
	</delete>
</mapper>